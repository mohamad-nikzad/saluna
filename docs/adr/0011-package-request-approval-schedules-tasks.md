# Package request approval schedules every task

Approving an AppointmentRequest for a Service Package must schedule every package task with assigned staff and time before the request becomes approved. We chose this over approving a package first and scheduling tasks later because an approved package without calendar work would be easy to lose operationally and would make staff dashboards incomplete.
